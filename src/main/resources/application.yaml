server:
  port: 8080

logging:
  level:
    com:
      hedvig: DEBUG

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/back-office
    username: postgres
    password: iambatman
    platform: POSTGRESQL
    driver-class-name: org.postgresql.Driver

  http:
      multipart:
        max-file-size: 512MB
        max-request-size: 512MB
  cloud:
    config:
      allow-override: false

  profiles:
    active: development

  jpa:
    hibernate:
      ddl-auto: update
      naming:
        strategy: org.hibernate.cfg.ImprovedNamingStrategy
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

intervals:
  tracker: 5000
  chat: 1000

websocket:
  max-sile-size: 5242880

tracker:
  baseUrl: http://localhost:4082
  urls:
    assets: /assets
    update: /asset
  stub: false

botservice:
  baseUrl: http://localhost:4081
  urls:
    messages: /messages
    internal:
      response: /_/messages/addmessage
      fetch: /_/messages
  stub: false

userservice:
  baseUrl: http://localhost:4084/member
  urls:
    users: /members
    userById: /
  stub: false

claims:
  baseUrl: http://localhost:4083
  urls:
    claims: /claims
    claimById: /claim
  stub: false

jwt:
  enabled: true
  expiration: 864000000
  secret: fGWPyfB8wBGoXB5B07xoa6KKfXobo79p
  header: Authorization
  param: token

cors:
  origins:
  methods: